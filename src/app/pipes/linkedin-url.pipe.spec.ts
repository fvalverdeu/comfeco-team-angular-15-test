import { LinkedinUrlPipe } from './linkedin-url.pipe';

describe('LinkedinUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkedinUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
